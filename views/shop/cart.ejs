<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (products.length > 0) { %>
                <ul>
                    <% products.forEach(p => { %>
                        <li><%= p.productData.title %> (Quantité <%= p.qty %>)</li>
                    <% }) %>
                </ul>
            <% } else { %> 
                <h1>Aucun produit !</h1>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>